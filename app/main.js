define(function (require) {
    
    var ko = require('knockout-3.4.0');
    var $ = require('jquery');
    var bootstrap = require('bootstrap');
    var leftPanel = require('./leftpanel');
    var boxArt = require('./utils/boxArt');
    var carousel = require('./bookShelf.js');
    var bookMedia = require('./utils/bookMedia.js');
    var checkoutBook = require('./checkoutBookList');
    var addnewBook = require('./addnewBook');
    var issuedBooks = require('./issuedBooks');
    //var addnewBooklist = require('.addnewBooklist');

    //var leftPanel = require('./utils/leftPanel');

    //console.log("HI i am in");

    //$('#wrapper').toggleClass('toggle')
});
